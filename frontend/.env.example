# Optional for local development. The app defaults to /api and Vite proxies that to the backend.
# For Vercel, replace this with your deployed backend URL, including the /api suffix.
VITE_API_BASE_URL=http://localhost:5000/api